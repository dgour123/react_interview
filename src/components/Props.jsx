import React from 'react'

const Props = ({name, age, pincode}) => {
  return (
    <div>
        <h1>My name iṡ {name} </h1>
        <h2>My age is {age}</h2>
        <h3>My area pin code is {pincode}</h3>
    </div>
  )
}

export default Props